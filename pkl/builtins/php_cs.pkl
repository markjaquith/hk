import "../Config.pkl"

class PhpCS extends Config.Step {
    glob = new { "**/*.php" }
    check = "phpcs {{ files }}"
    fix = "phpcbf {{ files }}"
    check_all = "phpcs ."
    fix_all = "phpcbf ."
} 
