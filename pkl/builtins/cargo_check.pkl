import "../Config.pkl"

class CargoCheck extends Config.Linter {
    glob = List("**/*.rs")
    check = "cargo check --all-features --all-targets"
    fix = "cargo check --all-features --all-targets --fix --allow-dirty"
} 
