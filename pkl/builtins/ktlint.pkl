import "../Config.pkl"

class Ktlint extends Config.Step {
    glob = new { "**/*.kt" }
    check = "ktlint {{ files }}"
    fix = "ktlint -F {{ files }}"
    check_all = "ktlint"
    fix_all = "ktlint -F"
} 
