import "../Config.pkl"

class Terraform extends Config.Step {
    glob = new { "**/*.tf" }
    check = "terraform fmt -check {{ files }}"
    fix = "terraform fmt {{ files }}"
    check_all = "terraform fmt -check"
    fix_all = "terraform fmt"
} 
