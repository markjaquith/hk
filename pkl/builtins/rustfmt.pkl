import "../Config.pkl"

class Rustfmt extends Config.Linter {
    glob = new { "**/*.rs" }
    check = "rustfmt --check --edition 2024 {{ files }}"
    check_list_files = "rustfmt --check --edition 2024 --files-with-diff {{ files }}"
    fix = "rustfmt --edition 2024 {{ files }}"
} 
