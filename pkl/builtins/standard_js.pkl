import "../Config.pkl"

class StandardJs extends Config.Step {
    glob = new { "**/*.{js,jsx,ts,tsx}" }
    check = "standard {{ files }}"
    fix = "standard --fix {{ files }}"
    check_all = "standard"
    fix_all = "standard --fix"
} 
