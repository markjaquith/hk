import "../Config.pkl"

biome = new Config.Step {
    glob = List("*.{js,jsx,ts,tsx,json}")
    stage = List("*.{js,jsx,ts,tsx,json}")
    check = "biome check {{ files }}"
    fix = "biome check --apply {{ files }}"
} 
