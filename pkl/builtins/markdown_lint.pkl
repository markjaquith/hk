import "../Config.pkl"

markdown_lint = new Config.Step {
    glob = "*.{md,markdown}"
    stage = "*.{md,markdown}"
    check = "markdownlint {{ files }}"
    fix = "markdownlint --fix {{ files }}"
} 
