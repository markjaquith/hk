import "../Config.pkl"

class Stylua extends Config.Step {
    glob = new { "**/*.lua" }
    check = "stylua --check {{ files }}"
    fix = "stylua {{ files }}"
    check_all = "stylua --check ."
    fix_all = "stylua ."
} 
