import "../Config.pkl"

class Eslint extends Config.Step {
    glob = new { "**/*.{js,jsx,ts,tsx}" }
    check = "eslint {{ files }}"
    fix = "eslint --fix {{ files }}"
    check_all = "eslint ."
    fix_all = "eslint --fix ."
} 
