import "../Config.pkl"

eslint = new Config.Step {
    glob = "*.{js,jsx,ts,tsx}"
    stage = "*.{js,jsx,ts,tsx}"
    batch = true
    check = "eslint {{ files }}"
    fix = "eslint --fix {{ files }}"
} 
