import "../Config.pkl"

class GoImports extends Config.Step {
    glob = new { "**/*.go" }
    check = "goimports -l {{ files }}"
    fix = "goimports -w {{ files }}"
    check_all = "goimports -l ."
    fix_all = "goimports -w ."
} 
